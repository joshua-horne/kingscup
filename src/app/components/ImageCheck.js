import React from 'react';
import {View, Image} from 'react-native';

const ImageCheck = () => {
  return (
    <View>
      <Image source={require('../../lib/AH.png')} />
    </View>
  );
};

export default ImageCheck;
